### User endpoints

#### POST /data/users
body {name, surname, birthdate, mail, password, phone (opt), parents[{name, surname, mail (opt), phone (opt)}]}
#### GET /data/users
#### GET /data/users/:uid